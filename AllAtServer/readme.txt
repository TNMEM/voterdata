These two files implement the "at server" style of fetch and page.

They work, but really exercise the databas ... too much.

It's better to fetch the searched data (with limits) and then display/page/sort.